import React from 'react'
const Resultado = ({descripcion}) => {
    return (  
        <div className="resultado">
            <p>{descripcion}</p>
        </div>
    );
}
 
export default Resultado;